TODO-NOTES.txt -- Claude's scratch pad for TODO.txt
===================================================

TODO.txt is the owner's list and is not edited by Claude. This file holds
working notes per item: where the code is, what is diagnosed, what to decide.
Numbers refer to TODO.txt.


1. Clicking a text file should open it in the internal viewer
-------------------------------------------------------------
Done (option a): Enter, or a click on the highlighted row, opens the viewer
for a text file. Binary files and provider items get a status message; mc's
"execute" is never done. Invoke-McOpenCurrent in src/Mc.PowerShell/Viewer.ps1;
Get-McViewablePath is the shared resolver F3 uses too. docs/COMPAT.md Enter
row is now "different". Tests: tests/Interface.ps1, "Enter on a leaf".
Found on the way: F3 on an Env: entry passed the old "is it a leaf" check,
because Test-Path -PathType Leaf is true for a variable. Fixed by checking
the provider.


2. Markdown rendering in the internal viewer for .md files
----------------------------------------------------------
Done. src/Mc.PowerShell/Markdown.ps1 is a pure converter: source lines ->
formatted lines of coloured, attributed segments, one per source line, so
line numbers, goto and search are untouched. The viewer paints segments
in both modes (plain mode is one segment per line). F9 toggles, matching
mc's Format key (F8 is mc's Raw/Parsed for hex, so not used).
Subset: ATX/setext headings, fenced and indented code, quotes, lists,
rules, tables, inline bold/italic/code/links/images, escapes. Italic is
underlined; the cell grid has no italic attribute.
Not done: nested lists beyond indentation, reference-style links, HTML.
Tests: tests/Interface.ps1, "Viewer: markdown".


3. VERSION.txt in the project root, updated with every version change
---------------------------------------------------------------------
Where: docs/VERSIONING.md says Mc.psd1 ModuleVersion is the single source of
       truth; tests/Lint.ps1 checks manifest, changelog and Get-McVersion agree.
Intent (owner, 2026-09-12): "know what I am running", plus a per-commit
       human-readable record of what changed and why.
Notes: the second intent is the commit log (git log); messages here already
       carry what and why. The first is better served by git describe than
       by a hand-written file: v0.5.0-3-ga7a68df names every commit with no
       action per commit. Proposed shape: mc -Version and Get-McVersion
       append git describe output when run from a checkout, bare manifest
       version otherwise. If a plain VERSION.txt is still wanted for
       something outside git to read, add it with a lint assertion that it
       equals ModuleVersion, and a step in docs/VERSIONING.md.
Status: not started; shape to confirm with the owner.


4. TODO-NOTES.txt as Claude's scratch pad
-----------------------------------------
Done: this file. Process recorded in CLAUDE.md.


Not on TODO.txt, diagnosed earlier -- registry navigation does not open keys
----------------------------------------------------------------------------
Symptom: in a registry panel, Enter/click on a subkey does nothing, silently.
Two faults:
  (a) PSProvider source's Descend uses Convert-Path, which returns the
      provider-internal path (HKEY_LOCAL_MACHINE\SOFTWARE\7-Zip), not a
      drive-qualified one. Nothing can handle it: FileSystem's Test wants a
      drive letter, Test-Path rejects it, Get-McPanelSource throws.
      Fix: build the child path from the current location
      (Join-Path $Location $Entry.Name) and only Convert-Path when the
      provider is FileSystem. Worth a helper next to Get-McParentPath in
      src/Mc.PowerShell/Sources.ps1; Cert: and module drives will hit the same.
  (b) The failure is silent. Set-McPanelLocation returns $false and reverts,
      but Invoke-McPanelEnter discards that with [void] and sets no message.
      Worth fixing regardless of (a).
Tests: tests/KeySequence.ps1 only descends on the filesystem. Add a provider
       case (descend into HKLM:\SOFTWARE\<key>, assert Location changed).
